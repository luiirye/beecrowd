{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Depurar arquivo C++ atual",
      "type": "cppdbg",
      "request": "launch",
      "args": [],
      "stopAtEntry": false,
      "externalConsole": false,
      "cwd": "${fileDirname}",
      "program": "${workspaceFolder}/build/${fileBasenameNoExtension}",
      "MIMode": "gdb",
      "preLaunchTask": "Compilar arquivo C++ atual"
    }
  ]
}
